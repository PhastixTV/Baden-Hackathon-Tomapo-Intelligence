services:
  ollama:
    image: ollama/ollama
    ports:
      - "11434:11434"
    volumes:
      - ollama_data:/root/.ollama
    command: /bin/sh -c "ollama serve & sleep 5 && ollama pull llama3.2 && wait"

volumes:
  ollama_data:
